#ifndef CLAM_UTILS_H
#define CLAM_UTILS_H

/* Check if an error occurred while allocating space to heap and crash the
 * program in that case */
void utils_checkheap(void* var);

#endif //CLAM_UTILS_H
